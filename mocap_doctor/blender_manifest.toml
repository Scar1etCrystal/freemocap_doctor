schema_version = "1.0.0"

id = "mocap_doctor"
version = "0.1.19"
name = "MoCap Doctor"
tagline = "Guided FreeMoCap cleanup for arue Teto and MMD VMD export"
maintainer = "MoCap Doctor contributors"
type = "add-on"

tags = ["Animation", "Rigging"]
blender_version_min = "4.3.0"
blender_version_max = "4.4.0"

license = ["SPDX:GPL-3.0-or-later"]

[permissions]
files = "Create project work files, checkpoints, reports, and VMD exports"

[build]
paths_exclude_pattern = ["__pycache__/", ".pytest_cache/", "tests/", "*.pyc"]
